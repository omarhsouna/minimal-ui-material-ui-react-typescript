import { createTheme } from '@mui/material/styles';
const theme = createTheme({
  palette: {
    primary: {
      main: '#fff',
      light: '#F0F6FF',
    },
    secondary: {
      main: '#19857b',
    },
  },
});
export default theme;
